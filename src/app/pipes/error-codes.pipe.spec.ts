import { ErrorCodesPipe } from './error-codes.pipe';

describe('ErrorCodesPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorCodesPipe();
    expect(pipe).toBeTruthy();
  });
});
